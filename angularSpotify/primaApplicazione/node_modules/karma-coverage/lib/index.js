// karma-coverage
// ==============
//
// Main entry point for the karma-coverage module.
// Exposes the preprocessor and reporter plugins.

// Registering one additional (karma specific) reporter: in-memory
require('karma-coverage/lib/report-creator').register(require('karma-coverage/lib/in-memory-report'))

module.exports = {
  'preprocessor:coverage': ['factory', require('karma-coverage/lib/preprocessor')],
  'reporter:coverage': ['type', require('karma-coverage/lib/reporter')]
}
